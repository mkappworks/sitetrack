apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  namespace: sitetrack
spec:
  selector:
    app: backend
  ports:
    - port: 3001
      targetPort: 3001
  type: ClusterIP  # internal only — Ingress handles external access
